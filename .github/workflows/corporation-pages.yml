name: Publish Illicit Corporation

on:
  push:
    branches: [main]
    paths:
      - index.html
      - manifest.webmanifest
      - builds.json
      - support/refresh-builds.cjs
      - .github/workflows/corporation-pages.yml
  workflow_dispatch:
  schedule:
    - cron: '17 * * * *'

permissions:
  contents: read

concurrency:
  group: corporation-pages
  cancel-in-progress: true

jobs:
  publish:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Prepare public static artifact
        run: |
          mkdir -p _site
          cp index.html manifest.webmanifest _site/
          touch _site/.nojekyll
      - name: Read deployed Omerta and Atlantis builds
        run: node support/refresh-builds.cjs _site
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
